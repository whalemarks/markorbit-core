# Book 02 MVP Gap Baseline Lock

Book 02 MVP Gap Baseline is locked only after this task passes validation.
Book 02 MVP remains incomplete.
Selected behavior-hook minimum-depth acceptance remains valid.
Domain business behavior remains unaccepted.
Execution System remains incomplete.
Production readiness remains unaccepted.

The baseline uses `whalemarks/markorbit-publication` at commit `3349ecb8955021a8714d023348f8b24f941eb98f` as authority. It maps the exact Must Build Now, Stub Now, Document Only, Defer, and Never in MVP inventories into `src/mvp-coverage` and validates current repository evidence without redefining Book 02 from implementation details.

Structural contract acceptance is not Book 02 MVP acceptance because a contract index entry or skeleton proves only that a boundary is named and documented. It does not prove real service behavior, API request and response validation, service delegation, exact event records, workflow preview/apply behavior, or executable test-family coverage.

Selected behavior-hook acceptance is also not execution-spine completion. Minimum-depth hooks for references, versions, idempotency, safety, governance, and AI context remain valid, but they do not complete Domain business behavior, the 18 Service runtimes, the 18 API validator/delegation boundaries, the three preview/apply Workflows, the exact 18 MVP Events, or five named Agent scaffolds.

Semantic overlaps are recorded as `semantic_overlap_only`. Generic Events such as task or communication catalog entries are not treated as exact replacements for `task-created`, `communication-reviewed`, or any other exact Book 02 Event unless a future governed task creates and validates an explicit canonical alias mapping.

Workflow skeletons do not equal preview/apply. The workflow catalog skeletons are structural evidence only; they do not validate preview requests, apply requests, Task plan generation, Human Review checkpoints, AI assistance boundaries, Permission and Policy validation, Idempotency, Event trace references, or safe error behavior.

Never in MVP violations are detected by the gap validator and report summary. A violation such as a full workflow engine, full policy engine, direct API Domain mutation, direct workflow Event emission, direct agent Event emission, production fixtures, raw database IDs in public responses, unsafe stack traces, or silent unsupported-version acceptance must produce `violation_present` and fail `pnpm gaps:mvp`.

Future implementation batches must consume this locked baseline by selecting unresolved dependency-ordered areas from the derived gaps. CORE-TASK-054 promotes Policy Service to owned Level 2–3 contextual-decision behavior after Permission Service. All 18 Must Build Services now meet the required behavior depth. CORE-TASK-055 locks the post-Service audit, and CORE-TASK-056 closes all 18 exact Event requirements: the current distribution is 68 meets depth, 3 partial, 39 validated skeleton and 5 boundary scaffold, with zero semantic overlap. Forty-seven Must Build requirements remain below required depth; 18 are MVP-accepted Domain skeletons and 29 are non-Domain completion blockers. Book 02 completion requires all 19 acceptance criteria plus required-depth evidence for every non-Domain Must Build requirement, without artificially promoting Domain skeletons into runtime behavior.
