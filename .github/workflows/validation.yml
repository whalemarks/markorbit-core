name: Validation

on:
  pull_request:
  push:
    branches:
      - codex/core-task-037-brand-service-core-lifecycle

permissions:
  contents: write

jobs:
  apply-object-validator-import:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout task branch
        uses: actions/checkout@v4
        with:
          ref: codex/core-task-037-brand-service-core-lifecycle
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply Object validator leaf import
        run: pnpm exec tsx scripts/core-task-037-fix-object-validator-import.mjs

      - name: Format governed files
        run: pnpm format

      - name: Commit Object validator import repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "CORE-TASK-037 — Use leaf validation result import"
          git push origin HEAD:codex/core-task-037-brand-service-core-lifecycle
